@using Pasaje5.Models;
@model List<ClienteCLS>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@Html.ActionLink("Agregar","Agregar","Cliente",null,new {@class = "btn btn-primary" })
<table class="table table-bordered table-hover">
    <thead>
        <th>@Html.LabelFor(p => Model[0].iidcliente)</th>
        <th>@Html.LabelFor(p => Model[0].nombre)</th>
        <th>@Html.LabelFor(p => Model[0].appaterno)</th>
        <th>@Html.LabelFor(p => Model[0].apmaterno)</th>
        <th>@Html.LabelFor(p => Model[0].bhabilitado)</th>
    </thead>
    <tbody>
        @foreach (var item in Model)
         {
            <tr>
                <td>@item.iidcliente</td>
                <td>@item.nombre</td>
                <td>@item.appaterno</td>
                <td>@item.apmaterno</td>
                <td>@item.bhabilitado</td>
            </tr>

         }
    </tbody>
</table>

